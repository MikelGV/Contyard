package data 
/**
    If the user uses podman(it should be something like contyard -p) it needs to open a new socket. But when the task
    is over(user closes contyard) that socket should be closed aswell
**/
func PodmanConStats() error {
    return nil
}

